
const { default: test } = require('node:test')
const tC = require('./index.js')

describe('Converting time to string', () => {
    test('if input is 0', () => {
        expect(0).toBe('0')
    })
    test('', () => {
        
    })
})